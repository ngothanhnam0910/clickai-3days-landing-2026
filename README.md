# clickai-3days-landing-2026

Static landing page (HTML/CSS/JS) with CTA **“Nhận lộ trình”**.

## Local preview
Open `index.html` directly in the browser (or use any static server).

## Deploy
Deployed on Vercel as a static site.
